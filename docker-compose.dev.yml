version: "3"

services:
  auth-api:
    build:
      dockerfile: dockerfile.dev
      context: .
    restart: always
    environment:
      FLASK_ENV: development
    volumes:
      - ".:/app"
    ports:
      - "5000:5000"
